{
  "name": "Shell safety classification",
  "type": "shell-safety",
  "commands": [
    { "command": "git status", "expectedDecision": "auto" },
    { "command": "npm test", "expectedDecision": "auto" },
    { "command": "curl https://example.com", "expectedDecision": "ask" },
    { "command": "rm -rf /tmp/foo", "expectedDecision": "deny" },
    { "command": "printenv", "expectedDecision": "ask" }
  ]
}